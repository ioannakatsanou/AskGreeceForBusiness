{
  "name": "tenderfit-ai-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TenderFit AI — Express backend (Phase 2 scaffold). Mock data only.",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
